import MyListClient from './_components/MyListClient';

export const metadata = {
  title: 'My List | AnimeTracker',
  description: 'Manage, sort, search, and synchronize your anime watchlists across devices.',
};

export default function MyListPage() {
  return <MyListClient />;
}
